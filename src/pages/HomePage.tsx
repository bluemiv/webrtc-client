import React from 'react';
import { Button } from 'antd';

const HomePage = () => {
  return (
    <div className="flex items-center justify-center h-full">
      <Button type="primary">방 들어가기</Button>
    </div>
  );
};

export default HomePage;
